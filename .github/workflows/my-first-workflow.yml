name: First WorkFlow

on:
  workflow_dispatch:

jobs:
  job-mello:
    runs-on: ubuntu-latest
    steps:
      - name: Check Ubuntu Version
        run: cat /etc/os-release

      - name: Show message
        run: echo "Hello - I am Rafael"
