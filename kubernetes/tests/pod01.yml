apiVersion: v1
kind: Pod 
metadata:
  labels: 
    app: nginx-simples
  name: pod-exec-test
spec:
  containers:
  - name: nginx-simples
    image: nginx 
    ports:
    - containerPort: 80
      hostPort: 80